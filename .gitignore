# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# Node
node_modules/
# 前端构建产物不入版本控制：每次构建文件名带 hash，提交 dist 会让每次功能改动
# 都产生几十个无意义的二进制 diff（历史上有 12/22 次提交在改 dist）。
# 构建请用 `npm run build`；start.bat / start.sh 会在 dist 缺失时自动构建。
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# 根目录 package-lock.json 属于本地 E2E 脚本（test_e2e.js，同样不入版本控制）的
# 依赖锁，对应的 package.json 已在 0ab8230 删除。没有 package.json 的 lockfile
# 对新克隆毫无用处 —— 实测 `npm ci` 直接 ENOENT 失败。前端 lockfile 仍受跟踪，
# 故用 / 锚定到根目录，避免误伤 frontend/package-lock.json。
/package-lock.json

# 覆盖率产物
coverage/
.coverage
htmlcov/

# Database
*.db
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.atomcode/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Environment
.env
.env.local
.env.*.local

# Test
tests/results/
playwright-report/

# Uploads
backend/uploads/*
!backend/uploads/.gitkeep

# Build
*.egg-info/
.eggs/

# 变更日志本地维护，不入版本控制（见 AGENTS.md）
CHANGELOG.md

# 本地 agent 指令文件不入版本控制：内含默认账号密码等开发环境信息，
# 而本仓库是公开仓库。文件保留在磁盘供本地 agent 使用。
AGENTS.md

# Screenshots / Test artifacts
screenshots/
nas_data/
docs/nas-config.md
test_e2e.js
.octto/
