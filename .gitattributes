# 换行符规范化
#
# 为什么需要它：start.bat 曾因 LF 换行而出错 —— cmd.exe 对 :: 标签做字节偏移
# 查找，在多字节 UTF-8（中文注释）下偏移算错，把注释尾部当命令执行。
# 只靠编辑器默认行为不可靠，这里显式钉住。
#
# text=auto：仓库内统一存 LF，检出时按平台转换
* text=auto

# Windows 批处理必须是 CRLF，且不要被当文本转换破坏
*.bat text eol=crlf
*.cmd text eol=crlf

# shell 脚本必须是 LF（CRLF 会让 shebang 解析失败）
*.sh text eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.docx binary
*.xlsx binary
*.zip binary
*.gz binary
*.tar binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary

# 锁文件：不参与 diff 统计，也不做换行转换
package-lock.json -diff linguist-generated
frontend/package-lock.json -diff linguist-generated

# 构建产物与生成文件不计入语言统计
frontend/dist/** linguist-generated
backend/alembic/versions/** linguist-generated
